# Session c892c382 — AI Kit spec review and iterative refinement with three design agents

- **Date range:** 2026-04-03 to 2026-04-03
- **Model:** `claude-opus-4-6`
- **User prompts:** 54
- **Commits produced:** 0

## Summary

Lane directed a thorough review and iterative refinement of `docs/ai-kit.md` by the Plan 9 and BeOS design agents. The session proceeded section by section, with Lane providing specific design direction at each step: clarifying that agents are named by their identity (`com.pane.ai.agent.<name>`) rather than their role, detailing the interactive permission-approval UI (a notification pane with approve/deny/modify/clarify options), defining a Unix user-group model for agent permissions (an `agent` group and a `builders` group), specifying that the agent model field belongs in the `.access` file not a routing section, and restructuring the "model access" and "routing" concepts as separate concerns. Lane also directed cuts to underspecified sections, defined a namespace format convention (`com.pane.ai.agent.ada`), and requested a final three-agent review followed by a staleness check. The session produced no commits — it was pure design deliberation feeding into subsequent sessions.

## Key directives

- Have the Plan 9 and BeOS agents review `docs/ai-kit.md` and make proposals for improvement.
- Clarify agent identity naming: use `com.pane.agent.<name>` (agent identity), not `com.pane.agent.builder` (role).
- Specify the permission approval workflow: mail displayed in an interactive notification pane allowing the user to approve, deny, respond with clarification, or set multiple permissions at once to avoid repeated interruptions.
- Define a `builders` group and `agent` group for Unix-level permission delegation; a particular agent may be added to `builders` when it needs CI tools access.
- Clarify that the agent's LLM model is specified in its `.access` file and parsed mechanically by the supervisor at launch — no routing needed for this.
- Identify the "model access/routing" section as a category mistake: merge model-selection into the `.access` file section; keep routing as a separate, still-underspecified area about system event propagation.
- Cut the section on relationship to Plan 9's `cpu` as too indeterminate.
- Audit the `.plan` file section for mixing concerns: `.plan` is for agent self-description, `.access` governs permissions — the section was conflating them.
- Use `ada` as a generic example agent name to avoid implying `builder` is the default agent form.
- Namespace format: `com.pane.ai.agent.ada` (pane → ai kit → agents → agent name).
- Do a staleness and conflicting-information review pass, then forward the revision to all three agents for final comment.
- Add an AI Kit summary to `README.md` as a "killer app" demonstration of the integrated system.

## Commits

(none)
