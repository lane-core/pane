# Session e0afb530

- **Date range:** 2026-04-02 to 2026-04-03
- **Model:** `claude-opus-4-6`
- **User prompts:** 92
- **Commits in range:** 26

## Commits produced

    94d9027 2026-04-02 21:17:11 -0700 Update README for redesign: status, building, tag description
    177b028 2026-04-02 21:11:40 -0700 Align pane-linux.md with .plan/.access split from AI Kit
    054a50e 2026-04-02 21:10:50 -0700 Split .plan/.access, fix communication framing, tools enforcement
    ec461bb 2026-04-02 21:01:30 -0700 Rewrite AI Kit §9 as sub-agent delegation, fix §2/§3/§10
    a4dbae7 2026-04-02 20:10:06 -0700 Add Pane Linux distribution design doc with open questions
    eac1383 2026-04-02 20:04:33 -0700 Surface unix multi-user research as AI Kit appendix
    984356b 2026-04-02 20:00:33 -0700 Remove spec-analysis.md
    8beb7b3 2026-04-02 19:49:14 -0700 Rewrite AI Kit communication/presence as unix-native + pane enrichment
    1b03a6b 2026-04-02 19:41:19 -0700 Detail unix multi-user primitive mappings in AI Kit
    12b1835 2026-04-02 19:36:36 -0700 Define pane exit notification as broadcast + filter opt-in
    1307bc1 2026-04-02 19:31:43 -0700 Add sandboxed agent compute section to AI Kit
    1b5bfdf 2026-04-02 19:28:44 -0700 Consolidate AI Kit design as docs/ai-kit.md
    3f4be5c 2026-04-02 19:10:40 -0700 Align distributed-pane.md with current architecture spec
    367f06e 2026-04-02 18:58:52 -0700 Separate tag, commands, and ctl in pane-fs hierarchy
    66e66e8 2026-04-02 18:42:49 -0700 Install legacy application wrapping design doc
    bbafc66 2026-04-02 18:25:19 -0700 Define pane-fs numbering convention: numeric local IDs, UUID views
    fe881c7 2026-04-02 18:15:22 -0700 Scrub stale v1 references from docs and memories
    bcd952c 2026-04-02 17:57:38 -0700 Address prior use-cases review: missing examples, I2, API fidelity
    106e2f5 2026-04-02 17:54:09 -0700 Address use-cases review: anti-pattern, phase tags, path conventions
    c744b6a 2026-04-02 17:44:47 -0700 Add use case scenarios grounding the architecture spec
    ebe819a 2026-04-02 17:38:03 -0700 Sharpen implementation details: ServiceId macro, filter ordering, CancelHandle routing
    1a54d00 2026-04-02 17:29:31 -0700 Fix send_request serialization bounds on msg and R parameters
    8afc6ff 2026-04-02 17:13:37 -0700 Address external spec review: 9 fixes to architecture.md
    e768105 2026-04-02 16:36:47 -0700 Align naming conventions and divergences with architecture spec
    af11805 2026-04-02 16:30:07 -0700 Add prototype subsystem inventory
    9dfa3d0 2026-04-02 16:25:05 -0700 Strike v1 prototype, reframe spec and plan as standalone

## Prompts

### `2026-04-02T23:09:09.324Z`

> hello! read the architecture spec docs/architecture.md in relation with the following:
> 
> we are engaging in a refactor of our v1 prototype. Because we spent a substantial amount of time going over this respecification and tightening up this specification, we want to be sure we follow our original intention and prepare well for this new effort. This particularly means we need to make the course of the development of this new model as clear as possible.
> 
> 1. We do not want the design of v1 to have any misleading influence as to any particular detail of the new protocol/API implementation, which means we must be as sparing as possible to any trace of its design or specific implementation details of the old system. I am letting you read the doc as is, but our first task is to reformulate it as a standalone source of truth document which does not refer to a past "v1" except purely to the extent that we may refer to mistakes or alterations we made from an earlier version of the system as motivating factors for decisions we made in the current design spec. The particular elaboration of the system is made on its own account and not in relation to the previous verison, a standalone doc that is the proposal for a new from scratch system not the revision of an old one.
> 
> 2. We will need to do a systematic scrubbing of staleness from the repo, including all the components which will be substantially refactored (if we have to rewrite more than 20% of it, we should just strike it out), along with memory and documentary details in the current redesign branch.

### `2026-04-02T23:13:52.810Z`

> lets do the first part and I'll weigh in on the second

### `2026-04-02T23:13:57.344Z`

> [interrupted]

### `2026-04-02T23:14:09.564Z`

> lets do the first  and 3rd part and I'll weigh in on the second

### `2026-04-02T23:15:25.697Z`

> lets do the revision and reframe PLAN.md. lets archive the v1-specific memories, and strike along the lines of a. we're on a new branch, so its equally viable just to redo *everything* from scratch, and might even be preferable so we have a fresh base

### `2026-04-02T23:26:59.448Z`

> can we now go the other way, and attempt to make a summary doc explaining the high-level functionality that was implemented in v1 that we wish to preserve? the clipboard/undo subystems, for instance

### `2026-04-02T23:31:31.909Z`

> would a programmer starting work on the current repo have clear direction to observe the identifier and naming protocols? in fact, do these protocols conflict with the current architecture spec?

### `2026-04-02T23:32:29.594Z`

> yes

### `2026-04-03T00:02:59.215Z`

> look at ./spec_analysis.md and address the criticisms to our three agents (after analyzing it yourself)

### `2026-04-03T00:03:46.638Z`

> /Users/lane/src/lin/pane/spec-analysis.md

### `2026-04-03T00:08:05.286Z`

> yes

### `2026-04-03T00:23:13.803Z`

> okay that resolved most of the issues but what about the remaining items in spec.md?

### `2026-04-03T00:23:59.568Z`

> can you give me a more expansive justification of 5,6,12,13 sourced from the analysis to give to my reviewer?

### `2026-04-03T00:28:03.236Z`

> their last concern has to do with "send_request serialization bounds" which is now incorporated in the updated doc

### `2026-04-03T00:37:16.399Z`

> here are some new things to take into consideration: Minor Implementation Notes (Non-blocking)
> ServiceId Derivation
> The spec notes ServiceId::new is not const fn due to UUIDv5 requiring SHA-1. Implementation strategy: Use a build-time proc-macro service_id!("com.pane.clipboard") that computes the UUID at compile time and generates ServiceId { uuid: Uuid::from_bytes([...]), name: "com.pane.clipboard" }. This avoids runtime initialization order issues.
> Filter Chain Ordering
> The spec states: "Filters run before handler dispatch... The filter chain is the first stage; handler dispatch is the second." Clarify ordering for transforms: If Filter A transforms Key → CommandExecuted, and Filter B matches on CommandExecuted, does B see the transformed message?
> Current text implies: Yes, filters are applied in registration order, and Transform produces a new Message that continues to the next filter. This is correct for composition (shortcut filters), but document explicitly: "Filter ordering matters: earlier filters see original messages; later filters see results of earlier transformations."
> CancelHandle Routing
> Ensure CancelHandle contains (ConnectionId, token) to route the Cancel message correctly. The text notes "Messenger embeds the routing context" — ensure this is explicit in the type definition for implementers.

### `2026-04-03T00:41:04.108Z`

> can we now clarify the example of the specification by imagining usecases arising from user applications in the future pane linux

### `2026-04-03T00:49:30.030Z`

> okay, now address these complaints:   Overall Assessment
>   This is a strong, motivating document that does what an architecture companion should do: it makes abstract
>   design decisions concrete and shows why they matter for real applications. The progression from terminal →
>   monitor → editor → file manager → chat → build system → AI guide effectively demonstrates that the architec
>   ture is general-purpose, not display-centric.
>   However, several use cases smuggle in concepts that are not defined in docs/architecture.md and one example
>   teaches an active anti-pattern. Below are specific findings.
>   ───────────────────────────────────────────────────────────────────────────────────────────────────────────
>   Per-Use-Case Feedback
>   1. Terminal emulator (pane-shell) — Contains an anti-pattern
>   Issue: proxy.set_pulse_rate(Duration::from_millis(16))? (60fps pulse) for a terminal emulator is wrong. A t
>   erminal should be event-driven — it redraws when the PTY fd becomes readable, not on a fixed timer. A 60Hz
>   pulse means 60 context switches per second even when idle, burning CPU for no reason. This is particularly
>   bad because pane-shell is labeled "the first real application" — developers will copy it.
>   Recommendation: Replace the pulse with a calloop source on the PTY fd (or however the Pty is integrated int
>   o the event loop). If you want to show set_pulse_rate somewhere, use the system monitor (#2) or a game/anim
>   ation where periodic update actually makes sense.
>   Minor issue: The downcast in request_received only checks for String and silently drops everything else:
>   if let Some(text) = msg.downcast_ref::<String>() {
>       self.pty.write_all(text.as_bytes())?;
>   }
>   drop(reply);
>   Since the spec now includes service: ServiceId, this example should demonstrate checking service first befo
>   re downcasting — that's the convention the architecture establishes.
>   Also: /pane/<id>/cons is introduced without definition. The architecture only discusses /pane/<id>/text. Is
>   cons a standard namespace path?
>   2. System monitor agent — Phase 3 feature in Phase 1 context
>   Issue: "Session suspension: the dashboard pane suspends when the user closes it" — session suspension is Ph
>   ase 3, but this use case reads as if it's available in Phase 1.
>   Recommendation: Add a (Phase 3) annotation when suspension is mentioned, or move this aspect to a Phase 3-f
>   ocused use case.
>   Also: /pane/monitor-web/ and /pane/monitor-db/ use human-readable names instead of pane IDs. How does the n
>   amespace map names to IDs? If this is an alias layer, it should be called out; if it's hypothetical future
>   functionality, label it as such.
>   3. Text editor with AI completion — Undefined concepts
>   This use case introduces several concepts not present in docs/architecture.md:
>   • RecordingOptic
>   • CoalescingPolicy
>   • DynOptic
>   • service_id! macro (the spec uses ServiceId::new)
>   • /pane/<id>/attr/cursor — the attr/ prefix convention
>   Recommendation: Either add these concepts to the architecture spec (if they are first-class), or add a side
>   bar/note in this document: "Optics details (RecordingOptic, DynOptic) are out of scope for the base archite
>   cture; see docs/optics-design-brief.md" (or wherever they live). The attr/ path convention in particular fe
>   els like it needs a namespace design document.
>   The service_id! macro is fine as syntactic sugar, but if it's used here, the architecture spec should at le
>   ast mention it exists.
>   4. File manager as query engine — Namespace syntax not specified
>   Issue: /pane/query/kind=image&modified>2026-03-01 introduces a query DSL in the filesystem path. This is co
>   mpelling, but it's not defined anywhere in the architecture. Is query a reserved top-level directory under
>   /pane/? Who parses the & and > operators? Is this pane-fs, a synthetic file server, or a higher-level conve
>   ntion?
>   Recommendation: Add a note that this is a pane-fs extension or synthetic file server, not core protocol beh
>   avior. Or link to a pane-fs design doc.
>   Also: RouteResult::MultiMatch and "quality rating" are routing details not in the architecture spec. If rou
>   ting quality scoring is part of the design, it belongs in the architecture doc.
>   5. Chat application — Strong example
>   This is the cleanest use case. It correctly demonstrates multi-server topology, per-Connection failure isol
>   ation, and cross-Connection causality through handler sequentiality. No major issues.
>   Minor: Session suspension is again mentioned without a Phase label.
>   6. Build system dashboard — Messenger::monitor is undefined
>   Issue: " Messenger::monitor: the editor pane monitors the build pane" — there is no monitor method in the a
>   rchitecture spec. This is a completely new API.
>   Recommendation: Either define monitor in the architecture spec (it sounds useful — something like proxy.mon
>   itor(pane_id, on_event)), or replace this with existing primitives. For example, the editor could use send_
>   request to register interest, or the build daemon could post app messages that the editor listens for via a
>   n application-defined protocol.
>   Also: /pane/build/cmd and /pane/build/attr/status again use the undefined attr/ and named-pane conventions.
>   7. The guide — Good, but .plan file is undefined
>   Issue: "Its .plan file governs what it can access" — .plan files are not defined in the architecture spec.
>   I know this is a superpowers/Claude Code concept, but for a reader of the architecture docs, this appears o
>   ut of nowhere.
>   Recommendation: Replace with: "Its sandbox policy (e.g., Landlock rules) governs what it can access" or add
>   a forward-reference to the superpowers system.
>   ───────────────────────────────────────────────────────────────────────────────────────────────────────────
>   Cross-Cutting Concerns
>   A. Namespace path conventions are inconsistent
>   The document invents several path conventions:
>   • /pane/<id>/text
>   • /pane/<id>/cons
>   • /pane/<id>/attr/...
>   • /pane/<id>/cmd
>   • /pane/query/...
>   • /pane/build/...
>   • /pane/monitor-db/... (no ID)
>   These are all reasonable, but none are defined in the architecture spec. If pane-fs path structure is part
>   of the architecture, it needs a dedicated section or a separate pane-fs.md spec. If it's application-define
>   d, say so explicitly.
>   B. Phase bleed
>   Phase 3 features (session suspension, RoutingHandler) appear in use cases that otherwise read as Phase 1/2.
>   I recommend tagging each use case with its primary phase, or at minimum adding (Phase 3) annotations to sus
>   pension and routing references.
>   C. The attr/ prefix convention
>   This keeps appearing (/pane/<id>/attr/cursor, /pane/<id>/attr/theme, /pane/<id>/attr/selection). If this is
>   a real convention in pane-fs, it should be documented. If it's just a stylistic choice in examples, it risk
>   s being treated as normative.
>   ───────────────────────────────────────────────────────────────────────────────────────────────────────────
>   Actionable Recommendations
>   1. Fix the terminal pulse rate. Remove set_pulse_rate(Duration::from_millis(16)) from pane-shell. Show PTY-
>      ven events instead.
>   2. Add service check to request_received example. Match the architecture spec's convention.
>   3. Create a pane-fs path convention doc (or add a section to the architecture spec) defining:
>     • /pane/<id>/text vs /pane/<id>/cons
>     • The attr/ prefix convention
>     • Whether /pane/query/... and /pane/<name>/... are core or extension behavior
>   4. Define or remove Messenger::monitor. If it's a real API, add it to the architecture spec. If not, rewrit
>      se case #6.
>   5. Phase-tag features. Add (Phase 3) to session suspension and RoutingHandler references so readers don't e
>      ct them in Phase 1.
>   6. Add forward-references for undefined optics concepts. RecordingOptic, DynOptic, CoalescingPolicy should
>      nt to docs/optics-design-brief.md or similar.
>   ───────────────────────────────────────────────────────────────────────────────────────────────────────────
>   Verdict
> 
>   The stories are right. Each use case validates a key architectural claim (headless-first, multi-server invi
>   sibility, namespace scripting, failure isolation). But the document currently mixes established architectur
>   e with hypothetical future features and undefined naming conventions without distinguishing them. A cleanup
>   pass to fix the terminal anti-pattern, tag Phase 3 features, and either define or forward-reference the nam
>   espace conventions would make this document as rigorous as the architecture spec itself.

### `2026-04-03T00:54:29.011Z`

> here is another review made prior to your revisions here, were these objections accounted for? This is doing the right job — it demonstrates that the architecture serves real applications, not just type-checks in the abstract. Let me go through what works, what's missing, and a few places where the examples reveal design questions the spec should eventually address.
> What works
> The shell (#1) is the right first example. It exercises the most components (Handler, DisplayHandler, Handles<Clipboard>, request_received with ServiceId, pane-fs), and the headless CI case makes the Handler/DisplayHandler split feel motivated rather than academic. "The same binary, the same protocol, the same Handler code" is the one-sentence pitch for headless-first.
> The editor (#3) is the best demonstration of application-defined protocols. The ModelMessage enum with Completion, DiagnosticReady, IndexingProgress is exactly the kind of real vocabulary a developer would invent. And the exhaustiveness point — "if the model adds RefactorSuggestion, the macro generates a match that fails to compile" — lands because it's concrete. You can imagine the actual CI failure.
> The chat client (#5) is the cleanest multi-server demonstration. Two Connections, one handler, the ServiceRouter routing invisibly. The failure isolation scenario (chat drops, display survives) is the kind of thing that would be a nightmare in a single-connection architecture and is free here. The suspension scenario (laptop closes, chat stays active, compositor suspends independently) demonstrates half-suspended state without making it sound exotic.
> The architectural themes section at the end is excellent. It extracts the design principles from the concrete examples without restating the spec. "The namespace is the scripting interface" is clearer here than in the foundations doc because you've just seen five examples of it.
> Where the examples reveal spec questions
> The shell's PTY integration raises an I2 question. The shell calls self.pty.write_all() in the key() handler. If the PTY's write buffer is full (the child process isn't reading), write_all blocks. This violates I2 (no blocking calls in handler methods). The real implementation would need non-blocking PTY writes with backpressure — write what fits, buffer the rest, flush on the next calloop writability event. The example glosses over this, which is fine for a use-case doc, but it's worth a note that PTY I/O requires a calloop source integration, not direct write_all.
> The shell's request_received downcasts on String without checking service.
> rustfn request_received(&mut self, proxy: &Messenger, service: ServiceId, msg: Box<dyn Any + Send>, reply: ReplyPort) -> Result<Flow> {
>     if let Some(text) = msg.downcast_ref::<String>() {
>         self.pty.write_all(text.as_bytes())?;
>     }
>     drop(reply);
>     Ok(Flow::Continue)
> }
> The ServiceId parameter was added specifically so receivers can dispatch on protocol identity before downcasting. This example ignores it — it accepts any String from any sender. The pattern should be:
> rustif service == service_id!("com.pane.shell.inject") {
>     if let Some(text) = msg.downcast_ref::<String>() { ... }
> }
> Small thing, but the use-case doc is where developers learn patterns. Getting this right here prevents a class of "accepts anything" bugs in downstream code.
> The file manager (#4) references features that aren't in the spec. RouteResult::MultiMatch, quality ratings, Sensitivity::Secret { ttl: 30s }, Locality::Local — these are all reasonable designs but none of them appear in the architecture spec. The clipboard metadata in the spec has Sensitivity::Normal and Locality::Any; the secret/local variants are invented here. If this doc ships alongside the spec, readers will expect these types to exist. Either define them in the spec (even as "future variants") or mark them explicitly as illustrative.
> The build daemon (#6) uses reply.reply(self.status.to_string()) — but ReplyPort's success method in the spec is reply.reply(payload) where the payload is Box<dyn Any + Send> (or serialized). The string-based convenience shown here implies a ReplyPort::reply(impl Into<Vec<u8>>) or similar sugar that doesn't exist yet. Minor, but the spec and the examples should agree on the ReplyPort API.
> The guide (#7) writes to /pane/editor/attr/theme — this is a cross-pane optic write through the filesystem. The optics design brief says the scripting protocol is Phase 3 (deferred), and cross-pane forwarding ("BApplication::ResolveSpecifier forwarded to a different looper via BMessenger::SendMessage") is explicitly deferred. The guide example assumes this works. It's fine to show the aspiration, but marking it as "Phase 3: RoutingHandler enables this" would prevent someone from expecting it in Phase 1.
> What's missing
> No example exercises the Dispatch callback pattern under adversity. The paste example in the spec (send_request with on_reply / on_failed) appears in the chat client implicitly but isn't shown. A use case that demonstrates a request failing — the clipboard server is down, on_failed fires, the handler degrades gracefully — would show the Dispatch system earning its keep. The monitoring dashboard (#2) could naturally include this: "the dashboard requests metrics from a monitored service; if the service is down, on_failed updates the status to 'unreachable' rather than crashing."
> No example shows the filter chain doing real work. The spec has a detailed filter chain design (MessageFilter, FilterAction, Transform/Consume), but no use case demonstrates it. The editor (#3) is a natural home: a keybinding filter that transforms Key(Ctrl+S) → CommandExecuted { command: "save" } before the handler sees it. This would show how the Input Kit (mentioned in the foundations doc) composes with the filter chain.
> No example shows CancelHandle. The Dispatch system's cancellation path (send a request, hold the CancelHandle, cancel if the user navigates away) is specified but never demonstrated. The editor's completion flow is perfect for this: send a completion request to the model, but if the user keeps typing, cancel the stale request so the model doesn't waste work on an outdated cursor position.
> No example shows post_app_message vs application-defined protocols. The spec has usage guidance ("when in doubt, use a Protocol"), but no use case shows the AppPayload path. A simple case: a worker thread that posts a "file saved" notification via post_app_message because it's a one-off event that doesn't warrant a full Protocol definition. Contrasting this with the editor's ModelProtocol would make the design guidance concrete.
> Structural suggestion
> The document is organized by application, which is right for developers who think "I want to build X, what do I use?" But it might also help to have a cross-cutting index at the end — not replacing the themes section, but supplementing it with a quick-reference:
> FeatureDemonstrated inHandler + DisplayHandler splitShell (#1), Monitor (#2)Handles<P> for servicesShell (#1), Editor (#3), Chat (#5)Application-defined ProtocolEditor (#3)Multi-server topologyMonitor (#2), Chat (#5), Build (#6)Dispatch + send_requestChat (#5)pane-fs namespaceShell (#1), Monitor (#2), File manager (#4), Build (#6), Guide (#7)RoutingHandlerBuild (#6)Session suspensionMonitor (#2), Chat (#5)Optics / scriptingEditor (#3), Guide (#7)
> Someone looking for "where do I see how multi-server works?" hits the table and goes straight to #2 and #5.

### `2026-04-03T01:03:16.157Z`

> with this more clarified view from considering concrete details, are there any stale attributes of our existing documentation and memories not in line with the new architecture?

### `2026-04-03T01:05:28.720Z`

> yes

### `2026-04-03T01:18:22.749Z`

> have we clarified the /pane/by-uuid/<uuid> file hierarchy distinction?

### `2026-04-03T01:19:13.903Z`

> can we take inspiration from /proc and /sys

### `2026-04-03T01:20:04.270Z`

> can we take inspiration from plan9 conventions and those influenced by them like /proc and /sys?

### `2026-04-03T01:20:14.396Z`

> can we take inspiration from plan9 conventions and those influenced by them like /proc, /sys, and /dev ?

### `2026-04-03T01:27:44.688Z`

> this was made before we resolved the pane-fs details we just did now, but it emerged from my dicatation of my general .app idea ("so the idea is that there is a fallback to run legacy apps and the compositor only makes the legacy app play nice in the runtime de. I dont expect an automated subsystem to attempt more complicated integration than that (although certain abstractions like a pane-gtk integration library or pane-dbus may go some distance towards it) -- rather I'd expect that (like in source-based linux distros) particular apps on the pane ecosystem layer or internally would maintain a specifically tuned pane wrapper. think of a .app folder like on NextStep but it contains a nix flake that, when the folder is dragged to the user's home app folder deploys the nix flake containing infrastructure on how to install the application (referring to packages to install from the underlying system's nixpkgs instance or from derivations hosted by the flake upstream source) with custom-tuned logic on how to best integrate the app into the pane environment
> 
> "): # Legacy Application Wrapping
> 
> How legacy applications (GTK, Qt, Electron, X11-via-Xwayland)
> participate in the pane ecosystem. This document specifies the
> compositor's role, the bridge process pattern, the `.app` bundle
> format, and the enrichment protocol that connects them.
> 
> Design principle: pane does not attempt to automatically extract
> rich semantics from legacy applications. The compositor provides
> a baseline (synthetic panes from Wayland surfaces). Per-app
> integration — the knowledge of how to best expose a specific
> application's state — lives in community-maintained `.app`
> bundles, following the source-based distribution model. pane
> provides composable tools; the ecosystem provides per-app
> expertise.
> 
> ---
> 
> ## 1. Synthetic Panes
> 
> When a Wayland client connects to pane-comp and creates an
> `xdg_toplevel`, the compositor creates a **synthetic pane** — a
> pane object with a UUID, a Tag, and namespace presence, managed
> internally by the compositor rather than by an external Handler
> process.
> 
> ### What the compositor knows from Wayland alone
> 
> The Wayland protocol provides:
> 
> | Wayland event | Pane state |
> |---|---|
> | `xdg_toplevel.set_title(s)` | `tag.title` |
> | `xdg_toplevel.set_app_id(s)` | `tag.signature` (already reverse-DNS by Wayland convention) |
> | Surface commit (buffer) | Display view (composited by pane-comp) |
> | Surface geometry + output scale | `Geometry { x, y, width, height, scale_factor }` |
> | `xdg_toplevel.close` / client disconnect | `PaneExited` |
> 
> The synthetic pane appears in the namespace:
> 
> ```
> /pane/<uuid>/tag/title       → "LibreWolf"
> /pane/<uuid>/tag/signature   → "org.librewolf.Librewolf"
> /pane/<uuid>/attrs/pid       → 48291
> /pane/<uuid>/attrs/synthetic  → true
> /pane/<uuid>/ctl             → accepts "close", "minimize", "maximize", "fullscreen"
> ```
> 
> `/pane/<uuid>/body` is empty for synthetic panes at baseline.
> The display view (pixels) is the compositor's rendering of the
> Wayland surface. Semantic content (text, structured data) is not
> available from the Wayland protocol — it arrives through
> enrichment (§3) if a bridge process provides it.
> 
> ### Compositor-internal representation
> 
> Synthetic panes have no external looper, no calloop event loop,
> no Protocol-speaking client process. The compositor manages them
> internally:
> 
> ```rust
> struct SyntheticPane {
>     id: Id,
>     tag: Tag,
>     geometry: Geometry,
>     surface: WlSurface,
>     toplevel: XdgToplevel,
>     pid: u32,                    // from wl_client credentials
>     enrichments: Vec<EnrichmentBinding>,  // from bridge processes (§3)
> }
> ```
> 
> The compositor translates Wayland events into pane state changes
> and emits the corresponding protocol events on the synthetic
> pane's behalf (Tag changes, PaneExited, geometry updates). It
> translates `/ctl` commands into Wayland protocol operations
> (`xdg_toplevel.send_close()`, configure events for minimize/
> maximize).
> 
> ### What synthetic panes cannot do
> 
> Synthetic panes are not full pane citizens. They lack:
> 
> - **Handler methods**: no `ready()`, `pulse()`, `command_executed()`.
>   The compositor provides a fixed command surface (`close`,
>   `minimize`, `maximize`, `fullscreen`) — not extensible without
>   a bridge.
> - **Handles\<P\>**: no typed service protocol participation. A
>   synthetic pane cannot open the clipboard service, respond to
>   scripting queries, or implement RoutingHandler. These require
>   a bridge process.
> - **Optic-governed properties**: the compositor exposes `title`,
>   `signature`, `pid`, and `geometry`. These are not DynOptic-
>   backed — they are direct translations from Wayland state.
>   GetPut/PutGet hold trivially (the compositor is the sole
>   writer), but the properties are not composable with the optic
>   layer's composition operators.
> 
> The gap between synthetic panes and native panes is the space
> that bridge processes fill.
> 
> ### Namespace convention
> 
> Synthetic panes share the namespace with native panes. They are
> distinguishable by `/pane/<uuid>/attrs/synthetic` (a boolean
> attribute), but this is metadata, not an architectural boundary.
> Scripts and tools that operate on `/pane/<uuid>/tag/title` work
> identically for synthetic and native panes.
> 
> The per-signature index works for synthetic panes:
> `/pane/by-sig/org.librewolf.Librewolf/` lists all LibreWolf
> instances (synthetic and native alike, if both exist).
> 
> ---
> 
> ## 2. Bridge Processes
> 
> A **bridge process** is a headless pane that enriches a synthetic
> pane with capabilities the Wayland protocol alone cannot provide.
> It is an ordinary pane — Handler, Messenger, protocol
> participation — whose purpose is to project knowledge about a
> specific legacy application into the pane namespace.
> 
> ### Lifecycle
> 
> ```
> pane-roster launches the bridge (from the .app service definition)
>   → bridge starts the legacy application (with configured env)
>   → legacy app connects to pane-comp as a Wayland client
>   → pane-comp creates a synthetic pane (automatic)
>   → bridge connects to pane-server as a headless pane
>   → bridge requests enrichment binding to the synthetic pane (§3)
>   → bridge populates attributes via its app-specific knowledge
> ```
> 
> The bridge process is a sibling of the legacy app, not a parent.
> If the bridge crashes, the legacy app continues (it's a Wayland
> client — the compositor keeps it alive). If the legacy app
> crashes, the bridge receives notification (process monitoring or
> D-Bus signal) and exits. The synthetic pane's enrichment
> attributes become stale and are cleared by the compositor when
> the enrichment binding is revoked.
> 
> ### What bridges know
> 
> A bridge author — someone who uses the application and
> understands its interfaces — encodes app-specific integration
> logic:
> 
> - **D-Bus properties**: many GTK/Qt apps expose state over D-Bus.
>   The bridge reads properties, maps them to pane attributes.
> - **CLI queries**: some apps support `--dump-state` or similar.
>   The bridge invokes these and parses output.
> - **Config file watching**: the bridge watches config files
>   (via inotify/fanotify) and exposes settings as attributes.
> - **IPC protocols**: apps with custom IPC (e.g., Firefox's
>   Marionette protocol, Emacs's `emacsclient`) can be driven
>   by the bridge.
> - **Command translation**: pane commands written to `/ctl` or
>   invoked through the command surface are translated by the
>   bridge into app-specific actions (D-Bus method calls, CLI
>   invocations, IPC messages).
> 
> ### Example: LibreWolf bridge
> 
> ```rust
> struct LibreWolfBridge {
>     dbus: DbusProxy,
>     target_pane: Id,
> }
> 
> impl Handler for LibreWolfBridge {
>     fn ready(&mut self, proxy: &Messenger) -> Result<Flow> {
>         self.dbus = DbusProxy::new("org.mozilla.firefox")?;
> 
>         // Declare enrichment for the synthetic pane.
>         // Publishes attributes and commands through the
>         // enrichment protocol (§3).
>         proxy.send_request::<Self, EnrichmentGrant>(
>             &self.server_messenger,
>             EnrichRequest {
>                 target: self.target_pane,
>                 properties: vec![
>                     PropertyInfo::new("url", ValueType::String)
>                         .operations(&[OpKind::Get]),
>                     PropertyInfo::new("tabs", ValueType::String)
>                         .operations(&[OpKind::Get]),
>                 ],
>                 commands: vec!["new-tab", "open"],
>             },
>             |bridge, proxy, grant| {
>                 bridge.enrichment_id = Some(grant.id);
>                 Ok(Flow::Continue)
>             },
>             |bridge, _| {
>                 // Server denied enrichment — degrade to no-op
>                 Ok(Flow::Stop)
>             },
>         )?;
> 
>         proxy.set_pulse_rate(Duration::from_secs(1))?;
>         Ok(Flow::Continue)
>     }
> 
>     fn pulse(&mut self, proxy: &Messenger) -> Result<Flow> {
>         // Poll D-Bus for current state.
>         // (Event-driven via D-Bus signal subscription is better;
>         // polling shown here for clarity.)
>         if let Ok(url) = self.dbus.get_property("URL") {
>             proxy.set_enrichment_attr("url", &url)?;
>         }
>         if let Ok(tabs) = self.dbus.call_method("GetTabs") {
>             proxy.set_enrichment_attr("tabs", &tabs)?;
>         }
>         Ok(Flow::Continue)
>     }
> 
>     fn request_received(&mut self, proxy: &Messenger, service: ServiceId, msg: Box<dyn Any + Send>, reply: ReplyPort) -> Result<Flow> {
>         // Commands routed from the synthetic pane's /ctl
>         if service == service_id!("com.pane.enrichment.command") {
>             if let Some(cmd) = msg.downcast_ref::<EnrichmentCommand>() {
>                 match cmd.name.as_str() {
>                     "new-tab" => { self.dbus.call_method("NewTab")?; }
>                     "open" => {
>                         if let Some(url) = cmd.args.get("url") {
>                             self.dbus.call_method_with_args("Open", &[url])?;
>                         }
>                     }
>                     _ => {}
>                 }
>             }
>         }
>         drop(reply);
>         Ok(Flow::Continue)
>     }
> }
> ```
> 
> The bridge is specific to LibreWolf. It knows about
> `org.mozilla.firefox` D-Bus interface, knows that `GetTabs`
> returns tab data, knows that `NewTab` opens a tab. This
> knowledge is maintained by the wrapper author, not by the pane
> framework.
> 
> ---
> 
> ## 3. The Enrichment Protocol
> 
> Enrichment is the mechanism by which a bridge process attaches
> attributes and commands to a synthetic pane it does not own.
> 
> ### Protocol definition
> 
> ```rust
> struct Enrichment;
> impl Protocol for Enrichment {
>     const SERVICE_ID: ServiceId = service_id!("com.pane.enrichment");
>     type Message = EnrichmentMessage;
> }
> 
> /// Messages from the server to the bridge.
> enum EnrichmentMessage {
>     /// A command was invoked on the synthetic pane's command
>     /// surface that belongs to this enrichment binding.
>     CommandInvoked { command: String, args: HashMap<String, String> },
>     /// The target synthetic pane was destroyed (app exited).
>     TargetLost,
>     ServiceLost,
> }
> ```
> 
> ### Binding lifecycle
> 
> 1. **Request**: the bridge sends `EnrichRequest { target, properties, commands }` via `send_request`. The server validates:
>    - The target pane exists and is synthetic
>    - The bridge's PeerAuth has permission to enrich this pane
>      (same uid, or explicitly granted via the pane's `.plan`)
>    - The property names don't collide with existing properties
>      or other enrichment bindings
> 
> 2. **Grant**: the server responds with `EnrichmentGrant { id }`.
>    The bridge holds this grant for the lifetime of the
>    enrichment. The granted properties appear in the synthetic
>    pane's namespace under `/pane/<uuid>/attrs/<name>`.
> 
> 3. **Updates**: the bridge writes attribute values via
>    `set_enrichment_attr(name, value)` — a Messenger method
>    scoped to the bridge's enrichment binding. The server
>    updates the synthetic pane's attribute set. Reads from
>    `/pane/<uuid>/attrs/<name>` return the bridge's last-written
>    value.
> 
> 4. **Commands**: when a user or script invokes a command that
>    belongs to the enrichment binding (e.g.,
>    `echo "new-tab" > /pane/<uuid>/ctl`), the server routes
>    the command to the bridge via `EnrichmentMessage::CommandInvoked`.
>    The bridge translates it into an app-specific action.
> 
> 5. **Revocation**: when the bridge disconnects (exit, crash,
>    `Flow::Stop`), the enrichment binding is revoked. The server
>    removes the enriched attributes from the synthetic pane's
>    namespace. The synthetic pane reverts to its baseline state
>    (Wayland-derived properties only). Revocation is also
>    explicit: the bridge can send `RevokeEnrichment { id }`.
> 
> ### Permission model
> 
> Enrichment is a write operation on another pane's namespace. The
> server enforces:
> 
> - **Same-uid default**: a bridge running as uid 1000 can enrich
>   synthetic panes whose Wayland client is also uid 1000. This
>   is the common case (user's bridge enriches user's apps).
> - **Cross-uid via `.plan`**: an agent running as a different uid
>   can enrich a pane if the pane's `.plan` (or the user's global
>   policy) grants `enrich` permission to that uid.
> - **Property collision**: two bridges cannot claim the same
>   property name on the same pane. First binding wins; second
>   request is declined with `EnrichmentDeclined::PropertyConflict`.
>   Bridges should namespace their properties (e.g., `dbus.url`,
>   `marionette.tabs`) to avoid collisions when multiple bridges
>   enrich the same pane.
> 
> ### Optic law status
> 
> Enrichment attributes are DynOptic-backed. The bridge's
> `set_enrichment_attr()` is a DynOptic `set()`; the namespace
> read is a DynOptic `get()`.
> 
> - **GetPut**: if the bridge writes a value and nobody else
>   writes, reading returns the written value. Holds.
> - **PutGet**: writing and then reading returns the written
>   value. Holds — the server stores exactly what the bridge
>   wrote.
> - **Fidelity to the legacy app**: the bridge's written value
>   may not reflect the legacy app's actual state if the bridge
>   is polling (stale between polls) or if the app rejects a
>   write-through (e.g., the bridge tries to set a URL but the
>   app navigates elsewhere). The optic laws hold for the pane's
>   attribute store, not for the bridge-to-app round-trip.
> 
> This is the "bridge is where type safety ends" boundary from the
> foundations doc. The enrichment protocol is typed and law-governed
> on the pane side. The bridge-to-app communication (D-Bus, CLI,
> IPC) is not.
> 
> ---
> 
> ## 4. The `.app` Bundle
> 
> A `.app` bundle is a directory containing a Nix flake that
> describes how to install a legacy application and integrate it
> with the pane ecosystem. It is the user-facing artifact for
> application deployment.
> 
> ### Structure
> 
> ```
> LibreWolf.app/
> ├── flake.nix              # deployment recipe (required)
> ├── flake.lock             # pinned dependencies (required)
> ├── metadata.toml          # human-readable app info (required)
> ├── icon.svg               # pane-native icon (optional)
> └── bridge/                # app-specific integration (optional)
>     ├── src/               # bridge process source (Rust, or any language)
>     ├── commands.toml      # command surface definition
>     └── properties.toml    # attribute mappings
> ```
> 
> ### metadata.toml
> 
> ```toml
> [app]
> name = "LibreWolf"
> signature = "org.librewolf.Librewolf"
> description = "Privacy-focused Firefox fork"
> license = "MPL-2.0"
> upstream = "https://librewolf.net"
> 
> [maintainer]
> name = "pane-apps contributors"
> contact = "https://github.com/pane-apps/librewolf"
> 
> [integration]
> # What level of pane integration this bundle provides.
> # "synthetic" = compositor baseline only (no bridge)
> # "bridge" = custom bridge process with enrichment
> level = "bridge"
> ```
> 
> ### flake.nix
> 
> The flake uses `pane-lib.wrapApp` to produce a wrapped package:
> 
> ```nix
> {
>   inputs = {
>     nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
>     pane-lib.url = "github:pane-project/pane-lib";
>   };
> 
>   outputs = { self, nixpkgs, pane-lib }:
>   let
>     system = "x86_64-linux";
>     pkgs = nixpkgs.legacyPackages.${system};
>     pane = pane-lib.lib.${system};
>   in {
>     packages.${system}.default = pane.wrapApp {
>       # The underlying application from nixpkgs.
>       app = pkgs.librewolf;
> 
>       # Pane service identity.
>       signature = "org.librewolf.Librewolf";
> 
>       # Bridge process (optional — omit for synthetic-only).
>       bridge = ./bridge;
> 
>       # Environment for the wrapped application.
>       env = {
>         MOZ_ENABLE_WAYLAND = "1";
>       };
> 
>       # Icon for the launcher / pane chrome.
>       icon = ./icon.svg;
>     };
> 
>     # pane service module: registers with the roster.
>     pane.services.${system}.librewolf = {
>       package = self.packages.${system}.default;
>       signature = "org.librewolf.Librewolf";
>       categories = [ "browser" "network" ];
>       autostart = false;
>     };
>   };
> }
> ```
> 
> ### Installation
> 
> Drag to app folder (or `pane app install ./LibreWolf.app`):
> 
> 1. The pane app manager evaluates the flake, building the
>    wrapped package and its transitive closure.
> 2. The package is added to the user's profile (Nix user
>    environment or Home Manager).
> 3. The service definition is registered with pane-roster.
> 4. The icon and metadata are indexed for the launcher and
>    pane-fs app directory.
> 
> The app is now launchable. On launch:
> 1. pane-roster starts the bridge process (if the bundle
>    includes one).
> 2. The bridge starts the legacy application with the
>    configured environment.
> 3. The legacy app connects to pane-comp (Wayland).
> 4. pane-comp creates the synthetic pane.
> 5. The bridge binds enrichment to the synthetic pane.
> 
> ### Uninstallation
> 
> Remove from app folder (or `pane app remove librewolf`):
> 
> 1. Running instances are sent `close` (graceful shutdown).
> 2. The service definition is removed from pane-roster.
> 3. The package is removed from the user's profile.
> 4. `nix store gc` cleans up unreferenced store paths.
> 
> ### Updates
> 
> The `.app` folder contains `flake.lock`. Updating:
> 
> ```
> pane app update librewolf   # runs nix flake update in the .app dir
> pane app update              # updates all installed .app bundles
> ```
> 
> The update rebuilds the wrapped package with newer nixpkgs
> (updated application) and/or newer pane-lib (updated bridge
> tooling). Running instances are not affected until restarted.
> 
> ---
> 
> ## 5. pane-lib: Wrapper Toolkit
> 
> `pane-lib` is a Nix flake providing the `wrapApp` function and
> supporting utilities for `.app` bundle authors. It is the
> composable toolbox, not a framework — wrapper authors use what
> they need.
> 
> ### wrapApp
> 
> ```nix
> pane.wrapApp {
>   app : derivation;       # the upstream package
>   signature : string;      # reverse-DNS service identity
>   bridge : path | null;    # path to bridge source (optional)
>   env : attrset;           # environment variables for the app
>   icon : path | null;      # icon file (optional)
>   wrapperScript : path | null;  # custom launch script (optional)
> }
> ```
> 
> `wrapApp` produces a derivation that:
> - Wraps the app's binary with the configured environment
> - Builds the bridge process (if `bridge` is provided)
> - Generates a launcher entry and roster service definition
> - Produces a systemd/s6 service file for the bridge process
> 
> If no bridge is provided, the app runs as a plain Wayland client
> and gets synthetic-pane treatment (§1) only.
> 
> ### Bridge helpers (Rust crate: pane-bridge)
> 
> `pane-bridge` is a Rust crate providing ergonomic wrappers for
> common bridge patterns. It depends on `pane-app` (the main SDK)
> and adds bridge-specific utilities:
> 
> ```rust
> use pane_bridge::{DbusAttr, FileAttr, CliCommand, BridgeBuilder};
> 
> fn main() -> pane_app::Result<()> {
>     BridgeBuilder::new("org.librewolf.Librewolf")
>         // Attributes sourced from D-Bus properties
>         .attr(DbusAttr::new("url")
>             .interface("org.mozilla.browser")
>             .property("URL")
>             .poll(Duration::from_secs(1)))
>         .attr(DbusAttr::new("tabs")
>             .interface("org.mozilla.browser")
>             .method("GetTabs")
>             .poll(Duration::from_secs(5)))
> 
>         // Attributes sourced from config files
>         .attr(FileAttr::new("profile")
>             .path("~/.librewolf/profiles.ini")
>             .parser(FileParser::Ini)
>             .key("Profile0.Name")
>             .watch(true))   // inotify, not polling
> 
>         // Commands translated to D-Bus calls
>         .command(CliCommand::new("new-tab")
>             .exec("librewolf --new-tab"))
>         .command(CliCommand::new("open")
>             .exec("librewolf {url}")
>             .arg("url"))
> 
>         .run()   // starts the bridge event loop
> }
> ```
> 
> `BridgeBuilder` generates a Handler implementation internally.
> For bridges that need custom logic beyond declarative mappings,
> authors implement Handler directly (as in §2's example).
> 
> ### Declarative bridge (TOML-only, no Rust)
> 
> For simple bridges where all attributes are D-Bus properties
> and all commands are CLI invocations, the bridge can be defined
> entirely in TOML — no Rust code:
> 
> ```toml
> # bridge/properties.toml
> [[property]]
> name = "url"
> source = "dbus"
> interface = "org.mozilla.browser"
> property = "URL"
> poll_seconds = 1
> 
> [[property]]
> name = "profile"
> source = "file"
> path = "~/.librewolf/profiles.ini"
> parser = "ini"
> key = "Profile0.Name"
> watch = true
> ```
> 
> ```toml
> # bridge/commands.toml
> [[command]]
> name = "new-tab"
> exec = "librewolf --new-tab"
> 
> [[command]]
> name = "open"
> exec = "librewolf {url}"
> args = ["url"]
> ```
> 
> `pane-lib` includes a generic bridge binary (`pane-bridge-generic`)
> that reads these TOML files and runs the appropriate BridgeBuilder
> pipeline. This means simple wrappers require zero compiled code.
> 
> ### Wrapper authoring workflow
> 
> ```
> mkdir MyApp.app && cd MyApp.app
> pane app init --signature com.vendor.myapp
> # creates flake.nix template, metadata.toml, bridge/ skeleton
> 
> # Edit metadata.toml with app info
> # Edit bridge/properties.toml and bridge/commands.toml
> # Or write a custom bridge in bridge/src/
> 
> pane app build     # nix build, installs locally for testing
> pane app test      # launches the app, checks enrichment binds
> pane app publish   # submits to pane-apps community repo (if desired)
> ```
> 
> ---
> 
> ## 6. Compositor Responsibilities
> 
> The compositor's role in legacy wrapping is bounded. It handles
> Tier 0 (Wayland protocol translation) and delegates everything
> else to bridge processes.
> 
> ### What pane-comp does
> 
> - Implements the Wayland protocol (wl_compositor, xdg_shell,
>   wl_seat, xdg_decoration, wp_viewporter, and extensions as
>   needed for GTK/Qt compatibility).
> - Creates synthetic pane objects for each xdg_toplevel.
> - Translates Wayland state changes to pane state changes
>   (title, geometry, activation, close).
> - Routes `/ctl` commands for synthetic panes:
>   - Built-in commands (close, minimize, maximize, fullscreen):
>     translated to Wayland configure/close events.
>   - Enrichment commands: forwarded to the bound bridge process
>     via the enrichment protocol.
> - Manages enrichment bindings: accepts/revokes bindings,
>   maintains the enriched attribute set, cleans up on bridge
>   disconnect.
> - Renders synthetic panes alongside native panes (both are
>   surfaces to the compositor).
> 
> ### What pane-comp does not do
> 
> - No D-Bus introspection of legacy apps.
> - No accessibility tree scraping.
> - No process injection or library preloading.
> - No attempt to parse or interpret application content.
> - No per-toolkit special cases in the compositor codebase.
> 
> All app-specific knowledge lives in bridge processes, which are
> external to the compositor. The compositor's complexity budget
> for legacy wrapping is bounded by the Wayland protocol — a
> known, stable surface.
> 
> ### Xwayland
> 
> X11 applications run through Xwayland, which presents them as
> Wayland clients to pane-comp. Xwayland-mediated clients produce
> synthetic panes the same way native Wayland clients do. The
> Xwayland layer may provide less metadata (X11's `WM_NAME` and
> `WM_CLASS` are less structured than Wayland's `app_id`). Bridge
> processes for X11 apps may need to supplement via `xprop` or
> EWMH properties.
> 
> Xwayland support in pane-comp follows standard practice
> (wlroots-style rootless Xwayland or equivalent). No pane-specific
> X11 infrastructure.
> 
> ---
> 
> ## 7. Integration Levels
> 
> Every legacy application on pane falls into one of two
> integration levels, determined by whether a `.app` bundle with
> a bridge exists.
> 
> ### Synthetic only (no bridge)
> 
> Any Wayland client gets this automatically:
> - Appears in namespace (`/pane/<uuid>/...`)
> - Title, signature, pid, geometry exposed
> - Basic commands (close, minimize, maximize, fullscreen)
> - Participates in workspace layout, pane roster, pane-fs queries
> - No semantic content, no app-specific attributes or commands
> 
> Sufficient for: casual use of any Linux GUI application.
> 
> ### Bridge-enriched (with `.app` bundle)
> 
> Applications with a maintained `.app` bundle additionally get:
> - App-specific attributes (`url`, `tabs`, `playing`, etc.)
> - App-specific commands (`new-tab`, `open`, `pause`, etc.)
> - Semantic body content (if the bridge provides it)
> - Command surface integration (pane's command palette
>   includes the app's commands)
> 
> The depth of enrichment depends entirely on the bridge author's
> effort and the application's available interfaces. A bridge for
> an app with a rich D-Bus interface (e.g., MPRIS-compliant media
> players) can expose nearly everything. A bridge for an app with
> no external interface can only add CLI-based commands.
> 
> ### Incentive toward native
> 
> The integration levels create a natural incentive gradient:
> 
> ```
> Wayland client     → synthetic pane (free, automatic)
> Wayland + .app     → enriched synthetic (community-maintained)
> pane-native app    → full citizen (Handler, optics, typed protocols)
> ```
> 
> Each level adds capability. The jump from synthetic to enriched
> is low-cost (a `.app` bundle with TOML config). The jump from
> enriched to native is a rewrite — but it's a rewrite toward
> better architecture, not toward a proprietary API. The pane
> protocol is session-typed and open; the kit libraries are
> composable and well-documented.
> 
> The ecosystem expectation: most apps stay at synthetic or
> enriched. Apps that benefit from deep system integration
> (terminal, editor, file manager, media player) go native. This
> mirrors the source-based distro pattern: most packages use
> generic build systems; a few important ones have carefully
> maintained build recipes.
> 
> ---
> 
> ## 8. Relationship to Architecture Spec
> 
> This document depends on and extends the following architecture
> spec components:
> 
> | Architecture concept | Role in legacy wrapping |
> |---|---|
> | Handler (headless) | Bridge processes are headless panes |
> | ServiceId (reverse-DNS) | Wayland `app_id` maps directly to service signature |
> | pane-fs namespace | Synthetic panes appear at `/pane/<uuid>/` |
> | DynOptic + PropertyInfo | Enrichment attributes are DynOptic-backed |
> | DeclareInterest | Bridge declares `com.pane.enrichment` |
> | PeerAuth::Kernel | Bridge authenticated by uid; same-uid enrichment is default |
> | `.plan` governance | Cross-uid enrichment requires explicit permission |
> | Dispatch + send_request | Bridge requests enrichment grant from server |
> | ServiceTeardown (Control) | Enrichment revoked on bridge disconnect |
> | CancelHandle | Bridge can revoke enrichment voluntarily |
> 
> ### New protocol: `com.pane.enrichment`
> 
> The enrichment protocol (`com.pane.enrichment`) is the one new
> service this document introduces. It follows the existing
> Protocol + Handles\<P\> pattern:
> 
> ```rust
> struct Enrichment;
> impl Protocol for Enrichment {
>     const SERVICE_ID: ServiceId = service_id!("com.pane.enrichment");
>     type Message = EnrichmentMessage;
> }
> ```
> 
> The protocol is between a bridge process and the pane server.
> It does not affect the architecture spec's existing protocols
> or invariants.
> 
> ### New Nix infrastructure
> 
> The `.app` bundle format and `pane-lib.wrapApp` are Nix-level
> infrastructure. They depend on the pane flake architecture
> (described in `docs/distributed-pane.md` §6) but do not affect
> the runtime protocol or the architecture spec.
> 
> ### Phase mapping
> 
> | Component | Phase |
> |---|---|
> | Synthetic panes (compositor) | Phase 1 (compositor implementation) |
> | Enrichment protocol | Phase 2 (requires DeclareInterest) |
> | `.app` bundles + pane-lib | Phase 2 (requires enrichment protocol) |
> | pane-bridge crate | Phase 2 (built on enrichment protocol) |
> | TOML-only declarative bridges | Phase 2 (built on pane-bridge) |
> | pane-apps community repo | Post-Phase 2 (ecosystem, not core) |
> 
> ---
> 
> ## Sources
> 
> - NeXTSTEP application bundles (`.app` directories)
> - Nix flakes (RFC 0049)
> - Wayland protocol: xdg-shell (stable), wl_compositor, wl_seat
> - AT-SPI2 / D-Bus accessibility (reference, not dependency)
> - MPRIS D-Bus interface (example of rich app introspection)
> - Plan 9 synthetic filesystems (`/proc`, `/dev/cons`)
> - Gentoo ebuilds, Nixpkgs derivations (community maintenance model)

### `2026-04-03T01:32:45.605Z`

> yes they didnt have the context that you do, but we can salvage the general idea here. we can defer to our spec with regard to the conflicts you just indicated, or would you rather give your own take on this from scratch?

### `2026-04-03T01:37:13.157Z`

> [interrupted]

### `2026-04-03T01:37:14.662Z`

> go ahead

### `2026-04-03T01:55:31.492Z`

> is tag really specced as a flat file? we had an extensive discussion of what tag would be, inspired by modal interfaces: pressing `alt(/option)-;` turns the title bar into a prompt where you can write commands which proceed via a fzf completion search similar to programs like nvim and so on, and these tags would be given by an app developer. (on pane-headless, pane interfaces served via tui would have this keybind available to type pane-related comands in the tty via a more directly comparable interface like neovim

### `2026-04-03T01:56:53.966Z`

> yes update pane-fs

### `2026-04-03T02:03:52.686Z`

> nice! from our most recently updated files, architeture.md, use-cases.md, legacy-workflow.md, pane-fs, can we audit the staleness or ambiguity of other docs in comparison?

### `2026-04-03T02:06:17.698Z`

> can we flesh out the ai kit more formally in an ai-kit.md

### `2026-04-03T02:06:57.774Z`

> can we flesh out the ai kit more formally in an ai-kit.md and consolidate there? you understand the basic concept, yes? reutilize the multi-user infrastructure and pane's architecture to make a native-to-unix agent infrastructure

### `2026-04-03T02:07:14.905Z`

> can we flesh out the ai kit more formally in an ai-kit.md and consolidate there? you understand the basic concept, yes? reutilize the multi-user infrastructure of unix (.plan files, finger, who, etc) and pane's architecture to make a native-to-unix agent infrastructure

### `2026-04-03T02:08:07.958Z`

> [interrupted]

### `2026-04-03T02:08:32.035Z`

> before you review distributed-pane.md for details, can we make sure that it is not stale with respect to the new architecture in docs/architecture.md?

### `2026-04-03T02:10:48.290Z`

> is there any staleness that imparted to use-cases.md that is now clarified by our clarification of distributed-pane?

### `2026-04-03T02:19:37.972Z`

> okay. can we audit our documentary surface for staleness, sources of ambiguity, confusion, or other issues we've missed with respect to the new architecture

### `2026-04-03T02:26:24.200Z`

> yes lets do that. please reach out if theres any ambiguity but I believe I've well specified the idea in previous documents

### `2026-04-03T02:30:18.255Z`

> now lets get meta: on linux clients using kvm (qemu on macos) agents can run their *own* agent infra via a sandboxed environment that communicate with the context (the agent userspace) through pane's protocol

### `2026-04-03T02:32:30.803Z`

> great. btw with regard to use-cases.md did we address this:
> 
> ```
> 
>   Verdict
>   Two blockers before this document is ready:
>   1. Fix the I2 violation in pane-shell's request_received — replace write_all with enqueue_input (or equival
>      non-blocking API).
>   2. Define or remove the ExitBroadcaster / pane-exit-registration claim in use case #6. If pane monitoring r
>      ires an explicit API, that API must exist in the architecture spec.
> ```

### `2026-04-03T02:33:32.719Z`

> [interrupted]

### `2026-04-03T02:34:07.359Z`

> hey I want to stop you, it seems we've gotten too familiar with making implementation choices without my input. for your a/b above I was interested in b but you went with 'a' because of convention. what convention?

### `2026-04-03T02:35:36.348Z`

> what if b is sent regardless but that the pane implements a filter for what it opts into?

### `2026-04-03T02:35:58.832Z`

> yes

### `2026-04-03T02:38:43.626Z`

> can you now actually look at the archived unix multi-user infra doc to formulate in more concrete detail what the ai-kit's inheritance from this framework is? agents sending messages to each other's ptys with mesg, the .plan file (and finger as a discovery mechanism), and so on?

### `2026-04-03T02:39:06.394Z`

> [interrupted]

### `2026-04-03T02:39:11.782Z`

> keep going

### `2026-04-03T02:46:11.084Z`

> I think you're getting in a habit of running ahead of things and we're gonna need to resolve a workflow fix before we continue past this task. for now however, there was an extensive multi-user unix infra report in our research docs, did you find it? the whole idea of how to adapt to that is that pane provides a unified infrastructure available to each user. users use shells, which come equipped with ready-made terminal interfaces for inter-machine communication developed by the bright minds at Bell Labs. the idea is that agents are literally users with their own account, and they can use the same pane infra that the user does -- that's the point, their interfaces are the same as the user's. They can interact with the user as if the user was using a unix mainframe computer, but enriched with further dispatch mechanisms because pane's infrastructure is additive. That doesn't seem to be coming across to me -- it is like you are saying we're replacing all of that with pane-native infrastructure, when the pane idiomatic thing is to use pane as a way to expose, communicate, and regulate interactions with state -- just like the legacy-apps.md style does.

### `2026-04-03T02:50:06.690Z`

> Can you formalize that convention in memory?

### `2026-04-03T02:54:31.331Z`

> here is commentary:  The Framing Fix: It Works
>   The original framing error was treating the AI Kit as a pane-native abstraction layer for agent communicati
>   on — inventing concepts like "brief notifications" and "focused sessions" that sat parallel to unix rather
>   than atop it. The revision (8beb7b3) correctly reframes the document around real unix commands (write, talk
>   , mail, wall, mesg, vacation) with pane as an enrichment layer.
>   This is now a much stronger document because:
>   • It doesn't invent a parallel communication stack.
>   • It demonstrates the thesis that "agents are unix users" with concrete, historically grounded examples.
>   • The "pane enrichment" sidebars consistently show additive value (structured state via pane-fs, typed attr
>     utes via pane-store) without replacing the underlying unix mechanism.
>   ───────────────────────────────────────────────────────────────────────────────────────────────────────────
>   Where the Revision Still Falls Short
>   1. The examples rely on dead or extinct unix tools
>   The document's credibility is undermined by its reliance on commands that are effectively archaeological on
>   modern Linux:
>    Command            Status on modern Linux
>   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
>    write(1)           Still exists, but virtually unused since the 1990s
>    talk(1) / talkd    Dead. Not installed by default on any major distro; the protocol is obsolete
>    finger(1)          Not installed by default; considered legacy/deprecated
>    biff(1) / comsat   Dead. Replaced by desktop notification daemons decades ago
>   The document presents these as if they are current standard infrastructure:
>   ▌ "Agents use the real unix communication commands — write, talk, mail, wall, mesg — because they are rea
>   ▌ l unix users with real TTYs. These commands work out of the box."
>   This is not true for talk and finger, and misleading for biff/comsat. The framing is right, but the example
>   s need to be honest about resurrection.
>   Fix: Add an explicit disclaimer early in §3 and §7:
>   ▌ "These are the original unix conventions — some are no longer installed by default. pane recovers them:
>   ▌ pane write, pane talk, pane finger are first-class implementations that speak the same protocol and pro
>   ▌ duce the same output."
>   Without this, the document reads as nostalgia rather than engineering.
>   2. PaneExited vs. disconnected() — Still wrong in §9
>   ▌ "If the VM crashes, the agent receives PaneExited { reason: Disconnected } on the affected Connection —
>   ▌ per-Connection failure isolation."
>   This is incorrect per the architecture spec. A Connection drop triggers Handler::disconnected(), not PaneEx
>   ited. PaneExited is a message about another pane exiting, delivered to monitors that have registered intere
>   st.
>   Fix: Change to: "If the VM crashes, the agent's disconnected() callback fires on the affected Connection."
>   3. Landlock claim is still overstated
>   ▌ "What the .plan says is what the kernel enforces. There is no gap between specification and enforcement
>   ▌ — the .plan IS the security policy."
>   Landlock is voluntary per-process. The agent binary must call landlock_create_ruleset() to opt in. A compro
>   mised agent binary can skip this entirely. The kernel does not enforce .plan rules unless the process coope
>   rates.
>   Fix: Add the honest caveat:
>   ▌ "Enforcement requires the agent's launcher (the s6 service wrapper or .plan parser) to install Landlock
>   ▌ rules before exec. Landlock catches violations by the agent process, but a fully compromised binary tha
>   ▌ t never calls landlock_create_ruleset() is not restrained — which is why §9's VM isolation exists for u
>   ▌ ntrusted code."
>   4. .plan parser timing is unclear
>   ▌ "a .plan parser... This is a build-time tool (part of the agent's s6 service definition), not a runtime
>   ▌ service."
>   "Build-time" is the wrong word here. If the parser runs as part of the s6 service definition, it's launch-t
>   ime or service-initialization-time — it sets up Landlock and network namespaces before execing the agent bi
>   nary. "Build-time" implies it runs when the package is compiled or when the Nix closure is built, which wou
>   ld make runtime enforcement impossible.
>   Fix: Change "build-time tool" to "launch-time tool."
>   5. talk streaming tokens framing is strained
>   ▌ "Character-by-character — the user sees the agent 'thinking' (streaming tokens), the same intimacy that
>   ▌ talk provided between humans in 1983."
>   This is poetic but engineering-fiction. talk transmitted finished characters as users typed them; it did no
>   t "stream tokens" from a language model. The comparison stretches the historical analogy too far.
>   Fix: Soften to: "Character-by-character transmission means the user sees the agent's response appear increm
>   entally, similar to how talk displayed the other party's typing in real time."
>   6. Phase 2 features (pane-store) used without Phase annotation
>   §4 (Memory) and §7 (pane enrichment with pane-store queries) treat pane-store as already available. The Pha
>   se mapping table at the end correctly notes "Agent memory (pane-store queries) | Phase 2," but the narrativ
>   e body doesn't tag this.
>   Fix: Add (Phase 2) annotations in §4 and §7 where pane-store is mentioned.
>   7. Remote TLS → unix account mapping remains hand-waved
>   ▌ "Remote connections authenticate via TLS client certificate (PeerAuth::Certificate), mapped to a local
>   ▌ unix account based on its certificate subject."
>   Still no mention of how this mapping is stored or administered. Is there a pane-authd? A config file? LDAP?
>   Fix: Add one sentence or a forward-reference: "The mapping is configured in /etc/pane/identities.toml — see
>   docs/distributed-pane.md §4."
>   8. ExitBroadcaster / pane monitoring registration is unexplained
>   §1 says headless panes use "pane_exited for monitoring," but the architecture spec does not show how a pane
>   registers to monitor another pane's exit. Same issue as in use-cases.md.
>   Fix: Either define the registration API in the architecture spec, or rephrase here to: "Pane death notifica
>   tions are broadcast to interested observers via the protocol."
>   ───────────────────────────────────────────────────────────────────────────────────────────────────────────
>   What's Significantly Better
>   • §3 Communication is now the strongest section. Mapping write, mail, wall, mesg to agent communication pat
>     rns is genuinely insightful, and the "pane enrichment" subsections are well-balanced.
>   • §7 Presence and Discovery with explicit who/finger/w output examples makes the "agent as user" concept ta
>     ible.
>   • The "What This Is Not" section remains crisp and load-bearing.
>   • §9 VM recursion continues to be the most technically compelling part of the document.
>   ───────────────────────────────────────────────────────────────────────────────────────────────────────────
>   Verdict
>   The reframing from "pane-native abstractions" to "unix-native + pane enrichment" was the right call and it
>   succeeded. The document's thesis is now coherent and defensible.
> 
>   However, the execution leans too heavily on obsolete unix tools presented as current infrastructure, and th
>   ree technical inaccuracies (PaneExited vs disconnected, overstated Landlock, "build-time" parser) need corr
>   ection. Fix those, add explicit "resurrected conventions" language for talk/finger/biff, and this document
>   will be as rigorous as the architecture spec.

### `2026-04-03T02:59:40.276Z`

> can we commit the spec-analysis.md?

### `2026-04-03T03:01:50.990Z`

> where is the multi-user report

### `2026-04-03T03:03:32.740Z`

> yes, perhaps as an appendix of the ai-kit

### `2026-04-03T03:05:33.230Z`

> from the documents and specs we have can you present to me what your understanding of the eventual pane linux distribution, specifically a sixos-based nix flake?

### `2026-04-03T03:07:33.517Z`

> don't treat the current flake defs as definitive, they are only sketches

### `2026-04-03T03:08:21.383Z`

> we should write it up as a doc making very clear the open questions we need to resolve

### `2026-04-03T03:11:34.190Z`

> ok great. WRT to ai-kit.md what do you think about the following issues:
> 
> ```
> • 1. PaneExited used for Connection drop — A VM crash triggers Handler::disconnected(), not PaneExited. Fix §
>   2. Landlock claim overstates enforcement — Landlock is voluntary per-process; a compromised agent binary th
>      never calls landlock_create_ruleset() escapes it. Add the honest caveat in §2.
>   3. "Build-time tool" is wrong — The .plan parser runs at service launch, not package build time. Change to
>      unch-time tool" in §10.
>   4. Pane-exit monitoring registration undefined — No API shows how a pane registers to observe another pane'
>      eath. Define proxy.monitor_pane(id) in the architecture spec, or rephrase the claim.
>   5. TLS → unix account mapping is hand-waved — No mechanism is shown for how a certificate subject maps to a
>      cal uid. Specify the mapping (e.g., /etc/pane/identities.toml) in §1 or §2.
> ```

### `2026-04-03T03:14:45.823Z`

> [interrupted]

### `2026-04-03T03:16:00.453Z`

> it looks like you took my directive to give a list of open questions above as a general workflow. when there's ambiguity in a question like this, you dont decide on your own but you dont just rush the way through. we are to work collaboratively, you forward the matter for my attention and I will decide it then or explicitly instruct you to note it as an open question. please remember this

### `2026-04-03T03:16:23.609Z`

> [interrupted]

### `2026-04-03T03:17:07.644Z`

> in fact better yet, when you *do* run into ambiguious decision points, you must always first run it across our three agents for their input, and then forward to me if their consensus does not resolve the matter

### `2026-04-03T03:17:17.753Z`

> in fact better yet, when you *do* run into ambiguious decision points, you must always first run it across our three agents for their input, and then forward the dilemma as well as their input to me if their consensus does not resolve the matter

### `2026-04-03T03:18:16.208Z`

> run it across them, but have them vet the ai-kit spec more gneerally

### `2026-04-03T03:51:30.482Z`

> okay 1. makes sense, along the lines of `c`

### `2026-04-03T03:51:56.308Z`

> okay 1. makes sense, along the lines of `c`, and for 2. yes state the distinction, but can you run back the vm proposed workflow so I know you got it right?

### `2026-04-03T03:59:27.151Z`

> that's not quite it, I think we got scrambled up somewhere, and I did not specify things well enough and you got carried away with what was there. The VM idea was a strictly limited idea. The idea was that agents each own a regular system user, managed by pane linux's nix configuration (or the darwin variant of the flake) using native unix functionality to correctly provision the account. the agent in pane uses the native system interfaces: here the idea of using pane connections directly by agents is appealing, and we can discuss that. the VM idea was more of a way to recurse this structure, so that agents can organize their own sub-agents whose goal is to perform tasks for them by having among their suite of tools a hypervisor they can manage (perhaps through pane-checked interfaces) in order to deploy agents (which also work the same way that they do, as a system with a multitude of user accounts) in order to perform subtasks, similar to how in agent assistants the master agent can delegate tasks to sub-agents. Does this make the idea clearer?

### `2026-04-03T04:00:13.377Z`

> yes

### `2026-04-03T04:03:01.166Z`

> I agreed with its assessment of the .plan and .access issue for 1, 2. proceed with the refinement of (c), and lets discuss 3 and apply 4

### `2026-04-03T04:03:26.061Z`

> what would the be engineer say?

### `2026-04-03T04:06:30.129Z`

> yes lets do that then

### `2026-04-03T04:14:12.790Z`

> okay can we now make this the master branch and move the old master to `proto-prototype`

### `2026-04-03T04:14:33.626Z`

> yes that's fine

### `2026-04-03T04:15:31.966Z`

> can we now update the README.md for staleness

### `2026-04-03T04:19:22.742Z`

> can you set origin to lane-core/pane

### `2026-04-03T04:19:39.904Z`

> can you set the current branch to upstream origin

### `2026-04-03T04:21:14.283Z`

> are there any aspects of pane-headless that be clarified by the new ai-kit specification? we expect that this workflow ought to be implementable in the pane linux way (which is what we spent our time meditating on) but there should be a version which deploys on darwin utilizing its native infrastructure the way nix-darwin does

### `2026-04-03T04:22:37.119Z`

> first do research for sandboxing solutions available on darwin including open source work

### `2026-04-03T05:04:05.683Z`

> [interrupted]

### `2026-04-03T05:04:09.028Z`

> everything alright?

### `2026-04-03T05:04:34.659Z`

> no try again

### `2026-04-03T07:08:44.446Z`

> [interrupted]

### `2026-04-03T07:08:45.257Z`

> well?

### `2026-04-03T07:09:15.615Z`

> [interrupted]

### `2026-04-03T07:09:17.442Z`

> <local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

### `2026-04-03T07:09:17.442Z`

> <command-name>/exit</command-name>
>             <command-message>exit</command-message>
>             <command-args></command-args>

### `2026-04-03T07:09:17.442Z`

> <local-command-stdout>Catch you later!</local-command-stdout>

### `2026-04-03T07:10:06.012Z`

> <local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

### `2026-04-03T07:09:47.808Z`

> <command-name>/compact</command-name>
>             <command-message>compact</command-message>
>             <command-args></command-args>

### `2026-04-03T07:10:14.877Z`

> <local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

### `2026-04-03T07:10:14.877Z`

> <command-name>/exit</command-name>
>             <command-message>exit</command-message>
>             <command-args></command-args>

### `2026-04-03T07:10:14.877Z`

> <local-command-stdout>See ya!</local-command-stdout>
