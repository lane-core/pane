[package]
name = "pane-hello"
version = "0.1.0"
edition = "2021"
description = "Hello pane — the canonical first pane application"
license = "BSD-2-Clause"

[dependencies]
pane-app = { path = "../pane-app" }
pane-proto = { path = "../pane-proto" }
