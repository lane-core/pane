[package]
name = "pane-app"
version = "0.1.0"
edition = "2021"
description = "The EAct actor framework for pane. Looper, Messenger, Pane, PaneBuilder, Dispatch."
license = "BSD-3-Clause"

[dependencies]
pane-proto = { path = "../pane-proto" }
# pane-session will be added when par-backed channels are built

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
