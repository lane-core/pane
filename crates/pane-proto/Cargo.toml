[package]
name = "pane-proto"
version = "0.1.0"
edition = "2021"
description = "Wire types and session protocol definitions for the pane desktop environment"
license = "BSD-3-Clause"

[dependencies]

[dev-dependencies]
