---
type: forwarder
status: superseded
note: psh content — canonical version is in psh's serena project, not in pane's
last_updated: 2026-04-10
---

# Forwarder

This is psh content, not pane content. The canonical version lives in psh's serena project.

For the historical pane snapshot (which used the older `try { } else e { }` syntax — psh has since refined this), see:

```
mcp__serena__read_memory("archive/psh/try_catch_typing")
```

For the current canonical version, switch to psh's serena project and read its memory store.

Will be swept on the next migration cycle.
